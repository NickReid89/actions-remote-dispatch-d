# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build:
    runs-on: ubuntu-latest
    steps:  
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      
      - name: test
        uses: NickReid89/workflow-templates/.github/workflows/directoryOne/action.yml@13c3dd72f2f667026233ab114b312081f2b6e665

      - name: test two
        uses: NickReid89/workflow-templates/.github/workflows/directoryTwo/action/yml@13c3dd72f2f667026233ab114b312081f2b6e665








